{
  "schemaVersion": 1,
  "id": "oneblock",
  "version": "${version}",
  "name": "OneBlock",
  "description": "A server-side OneBlock skyblock mod for Fabric 1.21.x",
  "authors": ["OneBlock"],
  "environment": "server",
  "entrypoints": {
    "main": ["com.oneblock.OneBlockMod"]
  },
  "mixins": ["oneblock.mixins.json"],
  "depends": {
    "fabricloader": ">=0.16.0",
    "fabric-api": "*",
    "minecraft": "~1.21"
  }
}
